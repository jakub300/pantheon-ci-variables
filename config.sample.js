const config = {
  url: '', // https://gitlab.xfive.co/api/v4/ https://gitlab.com/api/v4/
  privateToken: '',
  baseName: '', // user/repo
  pantheonGit: '',
  pantheonSiteName: '',
  pantheonTerminusToken: '',
  pantheonKey: `

`.trim(),
  baseKey: `

`.trim(),
};

module.exports = config;
